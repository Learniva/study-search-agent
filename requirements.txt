# Core LangChain & AI
langchain>=0.3.0
langchain-google-genai>=2.0.5  # Updated for Gemini 2.5 support
langchain-community>=0.3.0
langchain-experimental>=0.3.0
langgraph>=0.2.28

# Optional: Direct Google GenAI SDK (for compatibility)
google-genai>=0.2.0

# Search & APIs
google-api-python-client>=2.100.0  # Google Custom Search (Primary)
tavily-python>=0.5.0  # Tavily search (Fallback)

# Document Processing
docling>=2.0.0  # IBM Docling - Advanced document understanding with table extraction
pypdf>=4.0.0
pymupdf>=1.23.0  # PyMuPDF (fitz) - Fallback PDF text extraction
python-docx>=1.1.0
docx2txt>=0.9  # DOCX text extraction (reliable and maintained)

# Vector Store & Embeddings
chromadb>=0.4.22
pgvector>=0.3.0  # PostgreSQL vector extension for L2 Vector Store
langchain-postgres>=0.0.15  # LangChain PostgreSQL integration

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Utilities
python-dotenv>=0.21.0  # Constrained by manim-voiceover compatibility
pydantic-settings>=2.0.0

# Monitoring & Observability (Optional)
prometheus-client>=0.19.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0

# Caching & State Management (Optional)
redis>=5.0.0
hiredis>=2.2.0

# Animation (Optional)
manim>=0.19.0
manim-voiceover>=0.3.5
gTTS>=2.5.0

# Authentication & LMS Integration (Optional)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # Pin to 4.x for passlib compatibility
pylti1p3>=2.0.0
canvasapi>=3.2.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.100.0  # For Google Classroom API
authlib>=1.2.0  # OAuth client library
httpx>=0.24.0  # HTTP client for OAuth

# Payment Processing
stripe>=7.0.0

# Database (Optional)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
asyncpg>=0.29.0

# Testing
pytest>=7.4.0
